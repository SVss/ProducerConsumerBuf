#ProducerConsumerBuf

Главный поток создает 2 побочных потока.

1й поток берет из входного файла строку и добавл€ет ее в буфер; 2й поток берет из этого буфера строку и выводит еЄ в выходной файл, добавл€€ номер строки в начале.

1й поток завершаетс€, когда конец файла;
2й - когда в буфер больше не поступает данных.
Главный - после отработки двух побочных.